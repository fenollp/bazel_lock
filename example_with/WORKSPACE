workspace(name = "example_with")

local_repository(
    name = "bazel_upgrade",
    path = "..",
)

load("@bazel_upgrade//rules:upgrade.bzl", "http_archive")
load("//:LOCKFILE.bzl", "locked")

http_archive(
    name = "bazel_skylib",
    locked = locked,
    pin = "0.8.0",
    type = "tar.gz",
    url = "https://github.com/bazelbuild/bazel-skylib/releases/download/<pin>/bazel-skylib.<pin>.tar.gz",
)

load("@bazel_skylib//lib:versions.bzl", "versions")

versions.check(minimum_bazel_version = "1.0.0")
